{
  "name": "radha-naam-jap",
  "version": "1.0.0",
  "private": true,
  "description": "Radha Naam Jap - Panchanga / mala counter app (Capacitor Android wrapper)",
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:apk": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "build:apk:release": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "@capacitor/filesystem": "^6.0.1",
    "@capacitor/share": "^6.0.2",
    "@capacitor/browser": "^6.0.3",
    "@capacitor/app": "^6.0.2",
    "@capacitor-firebase/authentication": "^6.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
